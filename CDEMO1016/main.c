#include <stdio.h>
#include <string.h>
#include <stdbool.h>

// kernel-cmdline=root=/dev/sdaX rw nomodeset amdgpu.modeset=0 systemd.unit=multi-user.target

int main() {

  // char team[20] = "";

  // char *namn = "Antonio"; // readonly med char * = "string"
  // char namn2[] = "Antonio"; // read writable -> stack
  // char namn3[] = {'A', 'n', 't', 'o', 'n', 'i', 'o', 0};
  // printf("%s\n", namn);
  // printf("%s\n", namn2);


  // char str1[] = "Hello";
  // char str2[] = "Hello";

  // if (strcmp(str1, str2) == 0) { // String comparison
  //   printf("Strings aare the same");
  // }


  //   while(true) {
  //   char input[100];
  //   printf("Mata in vad du vill säga:");
  //   scanf(" %99s", input);

  //   if(strstr(input, "homework") || strstr(input, "vegetables")) {
  //     printf("You aare banned");
  //     break;
  //   }

  //   printf("Antonio says: %s\n", input);

    
  // }

  // printf("Don't come back until you have washed your mouth");


  // Iterera en sträng tecken för tecken
  // int index = 0;
  // int count = 0;
  // printf("%c\n", namn[0]);
  // printf("%c\n", namn[1]);
  // printf("%c\n", namn[2]);
  // printf("%c\n", namn[3]);
  // printf("%c\n", namn[4]);
  // printf("%c\n", namn[5]);
  // printf("%c\n", namn[6]);

  // // %s
  // while (namn[index] > 0) { // Sista char i array har värde 0 och loopen stoppar
  //   // printf("%c\n", namn[index]);
  //   index++;
  //   count++;
  // }


}